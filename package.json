{
  "name": "mi-sistema-administrativo",
  "version": "1.0.0",
  "private": true,
  "description": "Base pública del sistema administrativo con Supabase y Render.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test --test-isolation=none"
  },
  "engines": {
    "node": ">=20"
  }
}
